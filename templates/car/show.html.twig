{% extends 'base.html.twig' %}

{% block title %}Car{% endblock %}

{% block body %}
    <h1>Car</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ car.id }}</td>
            </tr>
            <tr>
                <th>LicensePlate</th>
                <td>{{ car.licensePlate }}</td>
            </tr>
            <tr>
                <th>VehIdNumber</th>
                <td>{{ car.vehIdNumber }}</td>
            </tr>
            <tr>
                <th>EngIdNumber</th>
                <td>{{ car.engIdNumber }}</td>
                <th>License_plate</th>
                <td>{{ car.license_plate }}</td>
            </tr>
            <tr>
                <th>Veh_id_number</th>
                <td>{{ car.veh_id_number }}</td>
            </tr>
            <tr>
                <th>Eng_id_number</th>
                <td>{{ car.eng_id_number }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('car_index') }}">back to list</a>

    <a href="{{ path('car_edit', {'id': car.id}) }}">edit</a>

    {{ include('car/_delete_form.html.twig') }}
{% endblock %}