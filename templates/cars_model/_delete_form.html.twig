<form method="post" action="{{ path('cars_model_delete', {'id': cars_model.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cars_model.id) }}">
    <button class="btn">Delete</button>
</form>