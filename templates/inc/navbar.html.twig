<nav class="navbar navbar-expand-sm navbar-dark bg-primary mb-3">

        <a href="/" class="navbar-brand">Symfony_demo</a>

        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#mobile-nav">
            <span class="navbar-toggle-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="mobile-nav">
            <ul class="navbar-nav ml-auto">
                <li class="nav-item">
                    <a href="/" class="nav-link">Home</a>
                </li>
                {#<li class="nav-item">#}
                    {#<a href="/client/new" class="nav-link">New</a>#}
                {#</li>#}
                {#<li class="nav-item">#}
                    {#<a href="/client/search" class="nav-link">Search</a>#}
                {#</li>#}
                {% if is_granted('ROLE_ADMIN')%}
                <li class="nav-item">
                    <a href="{{ path('admin_index') }}" class="nav-link">Admin</a>
                </li>
                {% endif %}
                {% if is_granted('ROLE_USER') %}
                    <li class="nav-item">
                        <a href="{{ path('fos_user_profile_show') }}" class="nav-link">Profile</a>
                    </li>
                    <li class="nav-item">
                        <a href="{{ path('service_index') }}" class="nav-link">Services</a>
                    </li>
                    <li class="nav-item">
                        <a href="{{ path('visit_index') }}" class="nav-link">Visits</a>
                    </li>
                    <li class="nav-item">
                        <a href="{{ path('fos_user_security_logout') }}" class="nav-link">Logout</a>
                    </li>
                {% else %}
                    <li class="nav-item">
                        <a href="{{ path('fos_user_security_login') }}" class="nav-link">Login</a>
                    </li>
                    <li class="nav-item">
                        <a href="{{ path('fos_user_registration_register') }}" class="nav-link">Register</a>
                    </li>
                {% endif %}
            </ul>
        </div>

</nav>