{% extends 'base.html.twig' %}

{% block title %}CarsBrand{% endblock %}

{% block body %}
    <h1>CarsBrand</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cars_brand.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ cars_brand.title }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('cars_brand_index') }}">back to list</a>

    <a href="{{ path('cars_brand_edit', {'id': cars_brand.id}) }}">edit</a>

    {{ include('cars_brand/_delete_form.html.twig') }}
{% endblock %}